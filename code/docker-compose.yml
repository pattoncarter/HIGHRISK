version: "3.8"

services:
  neo4j:
    image: neo4j:5
    container_name: neo4j
    ports:
      - "7474:7474"  # Browser GUI
      - "7687:7687"  # Bolt (Python connector)
    environment:
      - NEO4J_AUTH=none
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
